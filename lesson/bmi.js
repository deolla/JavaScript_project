/*
const markHeight = 1.69;
const markWeight = 78;

const johnHeight = 1.95;
const johnWeight = 92;

const markBMI = markHeight / markHeight ** 2;
const johnBMI = johnWeight / johnHeight ** 2;

const markHigherBMI = markBMI > johnBMI;

console.log(markHeight, markWeight, markBMI, markHigherBMI);
console.log(johnHeight, johnWeight, johnBMI, markHigherBMI);

const markHeight2 = 1.88;
const markWeight2 = 95;

const johnHeight2 = 1.76;
const johnWeight2 = 85;

const markBMI2 = markWeight2 / markHeight2 ** 2;
const johnBMI2 = johnHeight2 / johnWeight2 ** 2;

const markHigherBMI2 = markBMI > johnBMI;

console.log(markHeight2, markWeight2, markBMI2, markHigherBMI2);

if (markBMI > johnBMI) {
    console.log(`Mark's BMI ${markBMI} is higher that Jonh's ${johnBMI}!`)
} else {
    console.log(`John's BMI ${markBMI} is higher that Mark's! ${johnBMI}!`)
}

if (markBMI2 > johnBMI2) {
    console.log(`Mark's BMI ${markBMI2} is higher that Jonh's ${johnBMI2}!`)
} else {
    console.log(`John's BMI ${markBMI2} is higher that Mark's! ${johnBMI2}!`)
} */
